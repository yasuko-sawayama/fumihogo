const defaultState = {
  pageTitle: 'あいうえお',
};

const pageInfoReducer = (state=defaultState, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default pageInfoReducer;
